{
  "name": "plume",
  "version": "0.1.0",
  "private": true,
  "description": "Plume creative workflow platform",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
