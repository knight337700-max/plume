{
  "name": "@plume/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@plume/contracts": "workspace:*"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('No UI tests yet')\""
  }
}
