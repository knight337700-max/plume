apiVersion: plume.dev/v1
kind: StagingServiceContract
metadata:
  name: plume-staging
  environment: staging
  contractVersion: 1.0.0
  description: Platform-neutral service contract for the Plume MVP candidate.

# This is an interface contract, not a Kubernetes manifest. A platform adapter
# supplies the image registry, managed services, ingress, and secret provider.
images:
  web:
    repository: "<registry>/plume-web"
    tag: "${IMAGE_TAG}"
  api:
    repository: "<registry>/plume-api"
    tag: "${IMAGE_TAG}"
  worker:
    repository: "<registry>/plume-worker"
    tag: "${IMAGE_TAG}"
  scheduler:
    repository: "<registry>/plume-scheduler"
    tag: "${IMAGE_TAG}"

network:
  public:
    visibility: public
    services: [web, api]
    ingress:
      web:
        port: 8080
        tls: required
      api:
        port: 3000
        tls: required
  private:
    visibility: private
    name: plume-staging-private
    services: [api, worker, scheduler, postgres, redis, object-storage]
    denyDirectInternetIngress: true

services:
  web:
    role: browser-ui
    image: web
    networks: [public]
    publicEntry: true
    ports:
      http: 8080
    scaling:
      mode: horizontal
      minReplicas: 2
      maxReplicas: 4
      independentScaleKey: web
    resources:
      requests: { cpu: 100m, memory: 128Mi }
      limits: { cpu: 500m, memory: 512Mi }
    health:
      liveness: { type: http, path: /, port: 8080 }
      readiness: { type: http, path: /, port: 8080 }
    shutdown:
      signal: SIGTERM
      gracePeriodSeconds: 30
    config:
      API_ORIGIN: { fromConfig: PLUME_API_ORIGIN }
    forbiddenSecretInjection: [OPENAI_API_KEY, DATABASE_URL, REDIS_URL, S3_SECRET_ACCESS_KEY]

  api:
    role: http-api
    image: api
    networks: [public, private]
    publicEntry: true
    ports:
      http: 3000
    scaling:
      mode: horizontal
      minReplicas: 2
      maxReplicas: 8
      independentScaleKey: api
      independentFrom: [worker]
    resources:
      requests: { cpu: 250m, memory: 512Mi }
      limits: { cpu: 1000m, memory: 1Gi }
    health:
      liveness: { type: http, path: /api/v1/health, port: 3000 }
      readiness: { type: http, path: /api/v1/health, port: 3000 }
    shutdown:
      signal: SIGTERM
      gracePeriodSeconds: 45
      drainSseConnections: true
    secretRefs:
      - name: plume-staging-runtime
        keys:
          [
            DATABASE_URL,
            REDIS_URL,
            S3_ENDPOINT,
            S3_ACCESS_KEY_ID,
            S3_SECRET_ACCESS_KEY,
            S3_BUCKET,
            SESSION_SECRET,
          ]
    config:
      NODE_ENV: staging
      CORS_ORIGINS: "https://<staging-web-host>"
      COOKIE_SECURE: "true"
      COOKIE_SAME_SITE: lax
      QUEUE_PREFIX: plume-staging

  worker:
    role: async-job-consumer
    image: worker
    networks: [private]
    publicEntry: false
    scaling:
      mode: horizontal
      minReplicas: 2
      maxReplicas: 12
      independentScaleKey: worker
      independentFrom: [api]
    resources:
      requests: { cpu: 250m, memory: 512Mi }
      limits: { cpu: 2000m, memory: 2Gi }
    health:
      liveness: { type: process, entrypoint: worker-main }
      readiness:
        {
          type: queue-consumer,
          queues: [default, ai-high, ai-standard, render, validation, export],
        }
    shutdown:
      signal: SIGTERM
      gracePeriodSeconds: 120
      stopAcceptingJobs: true
      finishOrReleaseLeases: true
    secretRefs:
      - name: plume-staging-runtime
        keys:
          [
            DATABASE_URL,
            REDIS_URL,
            S3_ENDPOINT,
            S3_ACCESS_KEY_ID,
            S3_SECRET_ACCESS_KEY,
            S3_BUCKET,
            OPENAI_API_KEY,
          ]
    config:
      NODE_ENV: staging
      QUEUE_PREFIX: plume-staging

  scheduler:
    role: scheduled-maintenance
    image: scheduler
    networks: [private]
    publicEntry: false
    scaling:
      mode: singleton
      minReplicas: 1
      maxReplicas: 1
      policy: lease_singleton
      leaseName: plume-staging-scheduler
      leaseTtlSeconds: 90
    resources:
      requests: { cpu: 100m, memory: 256Mi }
      limits: { cpu: 500m, memory: 512Mi }
    health:
      liveness: { type: process, entrypoint: scheduler-main }
      readiness: { type: lease, leaseName: plume-staging-scheduler }
    shutdown:
      signal: SIGTERM
      gracePeriodSeconds: 60
      releaseLeaseBeforeExit: true
    secretRefs:
      - name: plume-staging-runtime
        keys:
          [DATABASE_URL, REDIS_URL, S3_ENDPOINT, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_BUCKET]
    config:
      NODE_ENV: staging
      QUEUE_PREFIX: plume-staging

  postgres:
    role: primary-database
    interface: postgresql
    serviceType: managed-or-persistent
    networks: [private]
    publicEntry: false
    port: 5432
    resources:
      capacityClass: managed-postgresql-staging
    persistence:
      required: true
      backupClass: daily-encrypted
      retention: 35d
    health:
      readiness: { type: tcp, port: 5432 }

  redis:
    role: cache-and-queue-backplane
    interface: redis
    serviceType: managed-or-persistent
    networks: [private]
    publicEntry: false
    port: 6379
    resources:
      capacityClass: managed-redis-staging
    persistence:
      required: true
      backupClass: provider-managed
      retention: 7d
    health:
      readiness: { type: redis-ping, port: 6379 }

  object-storage:
    role: asset-and-export-storage
    interface: s3-compatible
    serviceType: managed-or-persistent
    networks: [private]
    publicEntry: false
    port: 443
    resources:
      capacityClass: managed-object-storage-staging
    persistence:
      required: true
      backupClass: versioned-encrypted
      retention: 90d
    signedUrls:
      enabled: true
      host: "https://<staging-storage-host>"
      maxTtlSeconds: 900
    health:
      readiness: { type: object-storage, bucketRef: S3_BUCKET }

queue:
  interface: redis-compatible
  backingService: redis
  prefix: plume-staging
  delivery: at-least-once
  consumerIdempotency: required
  leaseSeconds: 120
  topics:
    [
      default,
      document-analysis,
      asset-processing,
      ai-high,
      ai-standard,
      render,
      validation,
      export,
      notifications,
      maintenance,
      dead-letter,
    ]

database:
  interface: postgresql
  backingService: postgres
  migration:
    owner: release-job
    command: pnpm db:migrate:test
    target: staging-database
    beforeTraffic: true
    rollback: restore-backup-and-forward-fix

secrets:
  provider: managed-secret-store
  environment: staging
  references:
    runtime: plume-staging-runtime
  requiredKeys:
    [
      DATABASE_URL,
      REDIS_URL,
      S3_ENDPOINT,
      S3_ACCESS_KEY_ID,
      S3_SECRET_ACCESS_KEY,
      S3_BUCKET,
      SESSION_SECRET,
    ]
  optionalKeys: [OPENAI_API_KEY]
  browserExposure: forbidden

observability:
  logs:
    format: json
    requiredFields: [timestamp, level, service, event, traceId, correlationId]
    redact:
      [authorization, cookie, set-cookie, database-url, storage-secret, openai-key, presigned-url]
  tracing:
    propagation: [traceparent, tracestate]
    exporter: otlp-http
    endpoint: "https://<staging-otlp-host>"
  metrics:
    endpoint: /metrics
    scrapeServices: [api, worker, scheduler]
    alerts:
      [
        api-5xx,
        queue-oldest-age,
        worker-crash-loop,
        export-failure-rate,
        database-capacity,
        dead-letter,
      ]

operations:
  deployment:
    strategy: rolling
    maxUnavailable: 0
    maxSurge: 1
    workerJobSafety: drain-or-release-lease-before-termination
    apiSseSafety: keep-old-api-until-connection-drain
  backups:
    postgres: { frequency: daily, retention: 35d, restoreDrill: monthly }
    objectStorage: { versioning: required, retention: 90d }
    redis:
      {
        frequency: daily,
        retention: 7d,
        note: queue data is replayable but persistence is required for recovery,
      }
  rollback:
    application: redeploy-previous-image-tag
    database: restore-backup-then-forward-fix
    worker: pause-consumers-before-schema-rollback
  smokeChecks:
    [
      web-tls,
      api-health,
      database-readiness,
      redis-readiness,
      storage-readiness,
      queue-consumer,
      sse-reconnect,
      signed-url-access,
    ]
